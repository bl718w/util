{
  "scripts": {
    "version:prod:up": "if git-branch-is develop; then cd scripts && ./version.sh; fi"
  },
  "pre-push": {
    "run": "version:prod:up"
  },
  "devDependencies": {
    "git-branch-is": "^2.1.0",
    "pre-push": "^0.1.1"
  }
}
